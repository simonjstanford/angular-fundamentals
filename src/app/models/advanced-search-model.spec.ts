import { AdvancedSearchModel } from './advanced-search-model';

describe('AdvancedSearchModel', () => {
  it('should create an instance', () => {
    expect(new AdvancedSearchModel(null,null,null,null,null,null)).toBeTruthy();
  });
});
